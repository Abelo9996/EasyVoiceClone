# Easy Voice Clone - Environment Variables Example
# Copy this file to .env and fill in your values (if needed)

# =============================================================================
# NOTE: This application does NOT require any API keys or external services!
# =============================================================================
# Voice cloning runs 100% locally using the Coqui XTTS-v2 model.
# No OpenAI, Google, or other cloud services needed.

# =============================================================================
# Frontend Configuration (React)
# =============================================================================

# Skip preflight check for Create React App
SKIP_PREFLIGHT_CHECK=true

# Disable host check (for development only)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Backend API URL (if different from default)
# REACT_APP_API_URL=http://localhost:5000

# =============================================================================
# Backend Configuration (Flask)
# =============================================================================

# Flask environment (development/production)
# FLASK_ENV=development

# Flask debug mode (true/false)
# FLASK_DEBUG=true

# Server host
# FLASK_HOST=0.0.0.0

# Server port
# FLASK_PORT=5000

# =============================================================================
# Optional: Model Configuration
# =============================================================================

# Custom model path (if you want to use a different XTTS model)
# TTS_MODEL_PATH=path/to/custom/model

# Enable GPU acceleration (auto-detected by default)
# USE_GPU=true

# =============================================================================
# Optional: Storage Paths
# =============================================================================

# Custom path for voice samples
# VOICES_DIR=./models/voices

# Custom path for output audio files
# OUTPUT_DIR=./output/app

# Custom path for temporary files
# TEMP_DIR=./temp

# =============================================================================
# Security (if exposing publicly - NOT RECOMMENDED without proper setup)
# =============================================================================

# Secret key for Flask sessions (generate with: python -c 'import secrets; print(secrets.token_hex())')
# SECRET_KEY=your-secret-key-here

# Enable CORS for specific origins
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Max file upload size (in MB)
# MAX_FILE_SIZE=100

# =============================================================================
# Rate Limiting (optional, for production)
# =============================================================================

# Requests per minute
# RATE_LIMIT=60

# Burst size
# RATE_LIMIT_BURST=10
